'use strict';

/**
 * assessment-password router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::assessment-password.assessment-password');
