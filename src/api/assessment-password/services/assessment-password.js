'use strict';

/**
 * assessment-password service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::assessment-password.assessment-password');
